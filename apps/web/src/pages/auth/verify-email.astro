---
import { VerifyEmailContainer } from '~/features/auth'
import Auth from '~/layouts/Auth.astro'

const token = Astro.url.searchParams.get('token')
---

<Auth title='Vérification email'>
  <VerifyEmailContainer client:load token={token} />
</Auth>
